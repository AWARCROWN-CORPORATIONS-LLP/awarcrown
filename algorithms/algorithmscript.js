function showAlgorithm(e){let n="";switch(e){case"mergeSort":n='\n                <h3>Merge Sort</h3>\n                <p>Merge Sort is a divide-and-conquer algorithm that splits the array into halves, sorts each half, and then merges them back together.</p>\n                <div class="code">\n                <pre><code>\ndef merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    result = []\n    left_index, right_index = 0, 0\n    while left_index < len(left) and right_index < len(right):\n        if left[left_index] < right[right_index]:\n            result.append(left[left_index])\n            left_index += 1\n        else:\n            result.append(right[right_index])\n            right_index += 1\n    result.extend(left[left_index:])\n    result.extend(right[right_index:])\n    return result\n                </code></pre>\n                </div>';break;case"bubbleSort":n='\n                <h3>Bubble Sort</h3>\n                <p>Bubble Sort repeatedly steps through the list, compares adjacent elements, and swaps them if they are in the wrong order.</p>\n                <div class="code">\n                <pre><code>\ndef bubble_sort(arr):\n    n = len(arr)\n    for i in range(n - 1):\n        for j in range(n - i - 1):\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]\n    return arr\n                </code></pre>\n                </div>';break;case"quickSort":n="\n                <h3>Quick Sort</h3>\n                <p>Quick Sort is an efficient, divide-and-conquer sorting algorithm that works by selecting a 'pivot' element and partitioning the array into two sub-arrays.</p>\n                <div class=\"code\">\n                <pre><code>\ndef quick_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    pivot = arr[len(arr) // 2]\n    left = [x for x in arr if x < pivot]\n    middle = [x for x in arr if x == pivot]\n    right = [x for x in arr if x > pivot]\n    return quick_sort(left) + middle + quick_sort(right)\n                </code></pre>\n                </div>";break;case"linearsearch":n='\n                    <h3>Linear Search Algorithm</h3>\n                    <p>Linear Search is a simple searching algorithm that searches each element in the list or array until the desired element is found or the end of the list is reached.</p>\n                    <div class="code">\n                    <pre><code>public static int LinearSearch(int a[], int key) {\n    for (int i = 0; i < a.length; i++) {\n        if (a[i] == key) {\n            return i + 1;\n        }\n    }\n    return -1;\n}</code></pre>\n\n                    <h2>Example Program:</h2>\n                    <pre><code>import java.util.*;\npublic class LinearSearch {\n    public static void main(String args[]) {\n        Scanner sc = new Scanner(System.in);\n        System.out.print("Enter the size of array: ");\n        int size = sc.nextInt();\n        int a[] = new int[size];\n\n        for (int i = 0; i < size; i++) {\n            System.out.printf("Enter the element %d: ", (i + 1));\n            a[i] = sc.nextInt();\n        }\n\n        Arrays.sort(a);\n        System.out.print("Sorted Array: ");\n        for (int i = 0; i < size; i++) {\n            System.out.print(a[i] + " ");\n        }\n\n        System.out.print("\\nEnter the element to search: ");\n        int key = sc.nextInt();\n\n        int result = LINEARSEARCH(a, key);\n        if (result != -1) {\n            System.out.printf("Element is found at position %d.\\n", result);\n        } else {\n            System.out.println("Element is not found.");\n        }\n    }\n\n    public static int LINEARSEARCH(int a[], int key) {\n        for (int i = 0; i < a.length; i++) {\n            if (a[i] == key) {\n                return i + 1;\n            }\n        }\n        return -1;\n    }\n}</code></pre>\n                    <h2>Input Example:</h2>\n                    <pre><code>Enter the size of the Array: 5\nEnter the element 1: 9\nEnter the element 2: 1\nEnter the element 3: 8\nEnter the element 4: 2\nEnter the element 5: 6\nSorted Array is:\n1 2 6 8 9\nEnter the element to search in: 8</code></pre>\n                    <h2>Output Example:</h2>\n                    <pre><code>Element is found at position 4.</code></pre>\n                </div>\n                </div>';break;default:n="<p>Select an algorithm to see details.</p>"}document.getElementById("algorithmContent").innerHTML=n}